rm -rf dist/{catalog,footer,header,main,profile,strrecept} 2>/dev/null; sudo chown -R www-data:www-data dist && sudo chmod -R 755 dist && echo "✓ Развёрнуто"


sudo systemctl restart nginx && sudo systemctl status nginx --no-pager